version: "3.9"

services:
  hp-gr:
    build: .
    container_name: hp-gr-site
    ports:
      - "4000:4000"
    volumes:
      - .:/usr/src/app
    environment:
      JEKYLL_ENV: production
    command: bundle exec jekyll serve --host 0.0.0.0 --port 4000
